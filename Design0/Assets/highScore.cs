using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.IO;

public class highScore : MonoBehaviour
{
   // string [] hScore;
    //string filePath, fileName;

   // [SerializeField] Text HighScore;


    ////////////////////
    //Tried to update scoreboard but had trouble reading from files: 
    ///////////////////
    
    /*


    // Start is called before the first frame update
    void Start()
    {
        fileName = "scores.txt";
        filePath = Application.dataPath + "/" + fileName;

        readFromFile();

        

        //TextMesh textObject = GameObject.Find("HighScore").GetComponent<TextMesh>();
        //textObject.text = hScore[0];
        //GetComponent(HighScore).text = "High Score: " + hScore;
    }

    public void readFromFile() {
        hScore = File.ReadAllLines(filePath);
    }



    */
}
